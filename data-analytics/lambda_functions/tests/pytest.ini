[pytest]
# tests live in lambda_functions/tests/ ; the Lambda is one level up.
# pythonpath is resolved relative to this file's directory (rootdir).
pythonpath = ..
markers =
    dbdata: tests that require a local Postgres loaded from req_add_info.csv
